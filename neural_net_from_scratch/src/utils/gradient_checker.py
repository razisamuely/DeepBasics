def check_gradient(func, x, epsilon=1e-7):
    """
    Verify gradients using finite differences.
    
    Args:
        func: Function that returns (loss, gradient)
        x (np.ndarray): Point at which to check gradient
        epsilon (float): Small perturbation for finite differences
        
    Returns:
        tuple: Analytical gradient, numerical gradient, and relative error
    """
    # TODO: Implement gradient checking
    pass